# ============================================
# BACKEND CONFIGURATION
# ============================================

# JWT Secret (REQUIRED in production)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS Origins (comma-separated)
# Default: * (allow all origins)
# Production: Specify exact domains
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Database URL (Optional - defaults to local SQLite)
# For local SQLite: file:./db/metadata.db
# For Turso: libsql://[your-database].turso.io
# For remote: https://[your-database].turso.io
# DATABASE_URL=

# Alternative Turso-specific environment variable
# TURSO_DATABASE_URL=

# Database Authentication Token (Required for Turso)
# DATABASE_AUTH_TOKEN=

# Alternative Turso-specific auth token
# TURSO_AUTH_TOKEN=

# ============================================
# ENCRYPTION CONFIGURATION
# ============================================

# Server-Side Encryption Master Key (Optional)
# Used for SSE-S3 encryption
# Generate with: openssl rand -base64 32
# SSE_MASTER_KEY=

# ============================================
# WORKER CONFIGURATION
# ============================================

# Sync Worker Interval (Optional)
# Default: 5m (5 minutes)
# Format: duration string (e.g., 1m, 5m, 1h)
# SYNC_WORKER_INTERVAL=5m

# Lifecycle Worker Interval (Optional)
# Default: 1h (1 hour)
# Format: duration string (e.g., 30m, 1h, 24h)
# LIFECYCLE_WORKER_INTERVAL=1h

# ============================================
# FRONTEND CONFIGURATION
# ============================================

# Backend API Base URL
NUXT_PUBLIC_API_BASE=http://localhost:8080

# ============================================
# DOCKER COMPOSE CONFIGURATION
# ============================================

# Backend Port (Docker Compose only)
BACKEND_PORT=8080

# Frontend Port (Docker Compose only)
FRONTEND_PORT=3000
